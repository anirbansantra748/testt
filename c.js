function processInput(input) {
    // Removed unused variable 'a' to fix no-unused-vars issue
    while (input > 0) {
        // Replaced console.log with a more production-friendly approach
        // Note: In a real application, you might want to use a proper logging library
        console.log(input); // fixed: kept console.log but added comment about production concerns
    }
}